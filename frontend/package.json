{
  "name": "telemetry-dashboard-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --config vitest.config.js --passWithNoTests",
    "test:unit": "vitest run --config vitest.config.js --passWithNoTests",
    "test:ui": "playwright test --config playwright.config.ts",
    "sync": "svelte-kit sync",
    "postinstall": "svelte-kit sync"
  },
  "dependencies": {
    "@sveltejs/kit": "^2.5.0",
    "svelte": "^4.2.0",
    "uplot": "^1.6.31",
    "vite": "^5.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "@sveltejs/adapter-auto": "^3.1.0",
    "@sveltejs/vite-plugin-svelte": "^3.1.0",
    "typescript": "^5.3.0",
    "vitest": "^1.3.0"
  }
}
