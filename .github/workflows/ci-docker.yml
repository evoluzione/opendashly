name: 🚀 CI/CD Pipeline

on:
  push:
    tags:
      - '[0-9]*'
    branches:
      - 'hotfix/**'
  workflow_dispatch:

jobs:
  test:
    name: 🧪 Test & Lint
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
      - name: 🐹 Set up Go Environment
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache-dependency-path: backend/go.sum
      - name: 🏃 Run Backend Tests
        working-directory: backend
        run: go test ./...
      - name: 🟢 Set up Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: 📦 Install Frontend Dependencies
        working-directory: frontend
        run: npm ci
      - name: ✨ Run Frontend Tests
        working-directory: frontend
        run: npm test

  docker:
    name: 🐳 Build & Push Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
      - name: 🛠️ Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: 🔐 Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.REGISTRY_HUB_SECRET }}
      - name: 🏷️ Generate Docker Metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/evoluzione/opendashly
          tags: |
            type=ref,event=tag
            type=raw,value=latest
      - name: 🏷️ Generate ClickHouse Metadata
        id: meta_clickhouse
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/evoluzione/opendashly-clickhouse
          tags: |
            type=ref,event=tag
            type=raw,value=latest
      - name: 🏷️ Generate OTEL Collector Metadata
        id: meta_collector
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/evoluzione/opendashly-otel-collector
          tags: |
            type=ref,event=tag
            type=raw,value=latest
      - name: 🚀 Build and Push image to GHCR
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
      - name: 🚀 Build and Push ClickHouse image to GHCR
        uses: docker/build-push-action@v6
        with:
          context: ./docker/clickhouse
          file: ./docker/clickhouse/Dockerfile
          push: true
          tags: ${{ steps.meta_clickhouse.outputs.tags }}
          labels: ${{ steps.meta_clickhouse.outputs.labels }}
      - name: 🚀 Build and Push OTEL Collector image to GHCR
        uses: docker/build-push-action@v6
        with:
          context: ./docker/otel-collector
          file: ./docker/otel-collector/Dockerfile
          push: true
          tags: ${{ steps.meta_collector.outputs.tags }}
          labels: ${{ steps.meta_collector.outputs.labels }}
